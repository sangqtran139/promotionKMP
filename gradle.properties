org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# compileSdk 35 vs AGP 8.2.0 (test tới 34) — chỉ là cảnh báo tương thích, tắt đi.
android.suppressUnsupportedCompileSdk=35

# Phiên bản artifact của SDK (dùng cho tên file AAR và BuildConfig)
SDK_VERSION=1.0.0
