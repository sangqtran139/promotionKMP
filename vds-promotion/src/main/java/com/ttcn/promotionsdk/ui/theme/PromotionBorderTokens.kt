package com.ttcn.promotionsdk.ui.theme

data class PromotionBorderTokens(
    // Card chi tiết ưu đãi (cardViewTop) — hiện tại _16dp
    val cardRadius: Float? = null,

    // Content sheet phía dưới màn list (bg_item_endow) — bo góc trên
    val sheetTopRadius: Float? = null,

    // FAB scroll to top — mini fab, bo tròn mặc định của Material
    val fabRadius: Float? = null
)